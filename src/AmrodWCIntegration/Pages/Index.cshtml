@page
@model IndexModel
@{
    ViewData["Title"] = "Home page";
}

<div class="text-center">
    <h1 class="display-4">Dashboard</h1>
</div>
<div>
    @*@foreach (var category in Model.Categories)
    {
        <p>@category.name</p>
    }*@
</div>

<div>
    @*@foreach (var amrodCategory in Model.AmrodCategories)
    {
        <p>@amrodCategory.CategoryName</p>
    }*@
</div>
